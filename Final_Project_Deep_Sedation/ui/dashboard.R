tagList(
  fluidRow(
    box(
      title = "Critical Care: Patient Exposure to Deep Sedation Across Ethnicities ",
      status = "success",
      solidHeader = TRUE,
      width = 12,
      h3(icon("share-alt"), "Welcome to the Critical Care Dashboard!"),
      p('To begin, click the data entry tab on the left. ')
    )
  )
)